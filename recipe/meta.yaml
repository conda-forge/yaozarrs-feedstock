{% set name = "yaozarrs" %}
{% set version = "0.3.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 7762411cd7afdaf513ea982d17a0549e51e5435224c2e9318e56c1cf9664f663

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  entry_points:
    - yaozarrs = yaozarrs._cli:main

requirements:
  host:
    - python {{ python_min }}.*
    - pip
    - hatchling
    - hatch-vcs
  run:
    - python >={{ python_min }}
    - pydantic >=2.5.0
    - typing-extensions >=4.6.0

test:
  imports:
    - yaozarrs.v04
    - yaozarrs.v05
  requires:
    - pip
    - python {{ python_min }}
  commands:
    - pip check

about:
  home: https://github.com/tlambert03/yaozarrs
  summary: 'Yet Another OME-Zarr Schema & Utils'
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  doc_url: https://tlambert03.github.io/yaozarrs/
  dev_url: https://github.com/tlambert03/yaozarrs

extra:
  recipe-maintainers:
    - tlambert03
